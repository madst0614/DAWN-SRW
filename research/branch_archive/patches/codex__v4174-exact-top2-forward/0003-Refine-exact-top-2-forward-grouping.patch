From 73f04c4894d656144c934a39bc43b5d854bb47a8 Mon Sep 17 00:00:00 2001
From: sh <madst0614@gmail.com>
Date: Sun, 26 Jul 2026 17:25:07 +0900
Subject: [PATCH 3/4] Refine exact top-2 forward grouping


diff --git a/EXPERIMENT.md b/EXPERIMENT.md
index 9e782b301f91af3914fbe23bd46b29ec6512adb4..4fcf4bedec2ed3b358f2f9b4faf1cc7172970179 100644
--- a/EXPERIMENT.md
+++ b/EXPERIMENT.md
@@ -12,8 +12,8 @@ Dates are KST. Experiment IDs are stable as `YYYY-MM-DD/#NN`.
 - Current conclusion: bundle4 dense forward, exact selected-top-2 backward, and the accepted analytic pullback remain unchanged. Generation `g3` at clean POC commit `8313cb07` establishes the independently keyed semi-orthogonal initializer as the current execution baseline: its two-layer F+B time was 0.726526 s and its five-step mean was 9.895410 s/it, within 0.20% and 1.16% of historical `g2`, respectively.
 - Baseline state: `docs/v4174_dense_rw_baselines.yaml` generation `g3` is active and pins the independent per-space initializer, exact POC commit, real-data protocols, runtime, mesh, and evidence paths. Generation `g2` is superseded and remains historical evidence for the old shared-base/row-sign-derived initializer.
 - Active run: the old-sign generation `g2` from-scratch workload was observed through step 400, archived, and replaced as authorized. Both `g3` benchmark protocols and the first exact-top-2-forward gate completed successfully; the TPU benchmark tmux wrappers are idle after completion.
-- Current blocker: the first exact top-2 forward implementation reduced combined two-layer forward by 10.83% but combined F+B by only 4.43%, below the mandatory 10% first gate. QKV exact forward regressed while RST exact forward improved, so the candidate is not eligible for full-step or skew-state measurement in this form.
-- Next experiment: keep the accepted analytic backward byte-for-byte on its existing all-space `Tcap=3072` primal, but screen one forward-only refinement that scans exact primary buckets in four-space groups to preserve the favorable bundle4 MXU shape without executing unselected spaces. Re-run the same two-layer gate; stop without full-step or skew-state work unless combined QKV+RST F+B reaches at least 10%.
+- Current blocker: the first exact top-2 forward implementation reduced combined two-layer forward by 10.83% and combined F+B by 4.43%; its full-step result is not yet measured. A forward-only four-space primary grouping refinement is locally validated but not yet TPU measured. Neither candidate has a user decision.
+- Next experiment: treat two-layer results as diagnostics rather than a discard gate. Run the first exact candidate and then the four-space forward refinement with the matched three-warmup/five-measure full-step protocol. Report full-step, compile, HBM, and skew evidence without making a keep/discard decision; that decision belongs to the user.
 
 ## Fixed Context
 
@@ -37,7 +37,7 @@ Dates are KST. Experiment IDs are stable as `YYYY-MM-DD/#NN`.
 
 - Machine-readable source: `docs/v4174_dense_rw_baselines.yaml`.
 - Canonical source branch: `codex/v4167-poc`. Candidate branches may produce comparison evidence, but a promoted implementation and its baseline decision must be synchronized to POC; the next canonical baseline run must use an exact clean POC-reachable commit.
-- Normal path: reuse the active generation's stored control and run only the candidate with the same two-layer gate. Continue to the matched three-warmup/five-measure full train benchmark only after the gate passes.
+- Normal path: reuse the active generation's stored control and run the candidate with the same two-layer diagnostic. For the current exact-forward comparison, the matched three-warmup/five-measure full train benchmark is the primary candidate criterion and is run regardless of the two-layer percentage; only the user makes the final keep/discard decision.
 - Promotion path: when a candidate passes both protocols and is accepted, its measurements become the next generation baseline directly. Promotion by itself never triggers a duplicate control run.
 - Rebaseline path: create one new baseline generation when accepted structure/shapes, forward or overflow semantics, precision/collectives, performance config or seed, data/batch/sequence, TPU/mesh, Python/JAX/Flax/Orbax/libtpu release line, or benchmark/profiler semantics changes. If accepted source changed without both protocol results, measure that accepted source once before testing the next candidate.
 - Noise path: near a threshold, repeat the candidate first. Repeat the control only after an invariant mismatch or concrete baseline-drift evidence.
@@ -527,6 +527,25 @@ Dates are KST. Experiment IDs are stable as `YYYY-MM-DD/#NN`.
 - Lesson/decision: exact bucketing alone is insufficient because the all-24-space primary batch regresses QKV forward despite reducing physical selected-space work; only RST realizes a material forward gain. Reject this exact execution shape and do not run its five-step or skew-state protocols. Preserve the candidate branch as evidence and try one forward-only four-space primary grouping refinement while leaving the accepted analytic backward unchanged.
 - Evidence: JSONL `/home/madst0614/DAWN-SRW/benchmark_runs/v4174_exact_top2_forward_candidate_1a1b8c71_gate/benchmark_metrics_host_6.jsonl`; archived raw log `/home/madst0614/train.v4174_exact_top2_forward_candidate_1a1b8c71_gate_20260726T1711KST.log` on all workers; local untracked launcher captures `launch_srw_benchmark_spatial-se-400m_20260726_170406_worker_{0..7}.log`; this notebook.
 
+#### #27 — Group exact forward primary buckets in four-space MXU batches
+
+- Status: implemented and locally validated on the candidate branch; TPU not yet measured.
+- Hypothesis/change: the first exact forward's all-24-space primary batch lost QKV efficiency even though it reduced physical space-token work. Keep the same exact per-space `Tcap=3072` packing and compact overflow, but scan primary buckets in six groups of four spaces so each dense contraction retains the accepted bundle4 MXU batch shape while every token-space pair remains semantically exact. This grouping is forward-only: the custom-VJP backward still invokes the existing all-space exact primal and accepted analytic RW pullback.
+- Local identity: branch `codex/v4174-exact-top2-forward`; clean parent `9a203dd0ca2732f8af0c0f90efd1883799e6feae`; dirty model SHA-256 `0baade943743fe5795d0e5f9cec10a7a5fe41f14104ca440b239cd986ae195d5`; trainer and config remain unchanged at SHA-256 `74c4305d0935561ace35b772598f760186346a17bab1d78d39f78e76b827347c` and `052dfda7315297ab93c8a8e2b9628c2fe16a0584fcba64f7cd34ee6e1c5c6b84`. No checkpoint, TPU, GCS, or remote execution was involved. Local environment: Python 3.12.7, JAX 0.6.2, Flax 0.10.7.
+- Implementation: attention and RST exact executors share their primary-space arithmetic between the unchanged all-space backward path and a static forward grouping path. The new forward path slices exact bucket metadata and every matching interface/controller/operator tensor in groups of four, executes no unselected space, accumulates exact writeback, then uses the unchanged compact overflow executor. Runtime contracts expose and fail-loud validate forward primary group size 4; `M=24`, `K=2`, bucket capacity, overflow grouping, parameter shapes/count, precision policy, collectives, routing, and backward remain unchanged.
+- Local validation: `py_compile` passed for the model, trainer, and TPU benchmark. On one-device FP32 reference kernels with `M=24`, group4 versus the previous group24 exact forward differed by at most `2.3842e-7`, with attention/RST relative L2 differences `5.9600e-8` and `4.6498e-8`. Linear losses were identical and every attention/RST input, controller, interface, operator-bank, routing, and scale gradient was bit-identical (`max_abs=0`, relative L2 `0`). The focused canonical parameter-count and trainer-factory contracts passed `2 passed in 44.85s`; `git diff --check` passed. No test file was created or modified.
+- Decision/next: publish the refinement, retain the two-layer result as a diagnostic, and run the matched three-warmup/five-measure full-step protocol as the primary comparison. Do not infer keep/discard from either metric; report the evidence for user decision.
+- Evidence: `models/dawn_srw_v4174.py`; local numerical/gradient validation output from 2026-07-26 KST; this notebook.
+
+#### #28 — Move exact-forward candidate judgment to the full-step protocol
+
+- Status: user-specified evaluation policy recorded; no new runtime measurement in this entry.
+- User decision/correction: the earlier 10% two-layer F+B threshold is no longer an automatic discard gate. Use matched three-warmup/five-measure full-step behavior as the primary candidate criterion, report results only, and reserve every keep, hold, or discard decision for the user.
+- Consequence: experiment #26 remains the exact historical diagnostic result, but its procedural `rejected` label does not constitute a candidate disposition. The all-24-space exact candidate remains available and must receive its missing full-step measurement. The group4 forward refinement is a separate follow-on candidate and will also be reported without an agent-authored disposition.
+- Reporting boundary: include two-layer QKV/RST timing, full-step mean/median/p90 and throughput, compile, peak HBM, validity/finiteness/process status, and step-100-skew behavior when measured. Distinguish unmeasured evidence explicitly and do not promote, merge to POC, or delete candidate work without a later user decision.
+- Next: run commit `1a1b8c7180891aa0704072264434cf0434df0326` with three warmups and five measured full steps, then measure the group4 refinement under the same protocol.
+- Evidence: user instruction in the active 2026-07-26 task; this notebook.
+
 ## Backfill Boundary
 
 - The initial notebook covered evidence-backed work from 2026-07-21 through the step-1500 resume attempt.
diff --git a/models/dawn_srw_v4174.py b/models/dawn_srw_v4174.py
index 2d44c778c84c070c0313cdbd08a8594ea6ca3416..055c779d7d0428aff423119dbd308624edaacf99 100644
--- a/models/dawn_srw_v4174.py
+++ b/models/dawn_srw_v4174.py
@@ -2280,6 +2280,7 @@ _BUNDLE_RW_RAW_OUT_CHECKPOINT_NAME = "v4174_bundle_rw_raw_out"
 _BUNDLE_RW_GATE_MASS_CHECKPOINT_NAME = "v4174_bundle_rw_gate_mass"
 _EXACT_SPACE_BACKWARD_BUCKET_CAPACITY = 3072
 _EXACT_SPACE_BACKWARD_TASK_GROUP_SIZE = 4
+_EXACT_SPACE_FORWARD_PRIMARY_GROUP_SIZE = 4
 _BUNDLE_BLOCK_CHECKPOINT_POLICY = (
     jax.checkpoint_policies.save_from_both_policies(
         jax.checkpoint_policies.dots_saveable,
@@ -4116,7 +4117,9 @@ def _make_sharded_attention_space_bundle_dense(
             controls,
             packed_metadata,
             primary_weight,
-            overflow_weight):
+            overflow_weight,
+            *,
+            primary_group_size: Optional[int] = None):
         (space_read_proj, space_write_proj,
          q_tau_kernel, q_tau_bias,
          k_tau_kernel, k_tau_bias,
@@ -4142,80 +4145,212 @@ def _make_sharded_attention_space_bundle_dense(
         route_scales = jnp.asarray(
             (qk_scale, qk_scale, v_scale), dtype=jnp.float32
         ).reshape((3, 1, 1))
-        primary_token = packed_metadata["primary_token_id"]
-        primary_valid = packed_metadata["primary_token_valid"]
-        primary_state = global_state[primary_token]
-        primary_local = projection_dot(
-            "mtd,mdr->mtr", primary_state, space_read_proj
-        ).astype(jnp.float32)
-        primary_local_norm = jnp.maximum(
-            jnp.linalg.norm(primary_local, axis=-1, keepdims=True),
-            jnp.float32(RW_FORWARD_NORM_EPS))
 
-        primary_qk_tau = (
-            _control_einsum_f32(
-                "mtr,amri->amti", primary_local, qk_tau_kernels)
-            + qk_tau_biases[:, :, None, :])
-        qk_raw_out, qk_gate_mass = _dense_rw_output_sharded(
-            primary_local,
-            primary_local_norm,
-            primary_valid,
-            qk_reads,
-            qk_writes,
-            primary_qk_tau,
-            max_chunk_size=max_chunk_size_qk,
-            soft_gate_temperature=qk_temperature,
-            soft_gate_boundary_power=boundary_power,
-            execution_prune_eps=execution_prune_eps,
-            srw_composition_mode=composition_mode,
-            heat_kernel_beta=heat_kernel_beta,
-            effective_active_eps=soft_gate_effective_active_eps,
-            throughput_bf16=throughput_bf16)
+        def execute_primary_spaces(
+                local_primary_token,
+                local_primary_valid,
+                local_primary_weight,
+                local_space_read_proj,
+                local_space_write_proj,
+                local_qk_tau_kernels,
+                local_qk_tau_biases,
+                local_v_tau_kernel,
+                local_v_tau_bias,
+                local_qk_reads,
+                local_qk_writes,
+                local_v_read,
+                local_v_write):
+            primary_state = global_state[local_primary_token]
+            primary_local = projection_dot(
+                "mtd,mdr->mtr", primary_state, local_space_read_proj
+            ).astype(jnp.float32)
+            primary_local_norm = jnp.maximum(
+                jnp.linalg.norm(primary_local, axis=-1, keepdims=True),
+                jnp.float32(RW_FORWARD_NORM_EPS))
+
+            primary_qk_tau = (
+                _control_einsum_f32(
+                    "mtr,amri->amti",
+                    primary_local,
+                    local_qk_tau_kernels)
+                + local_qk_tau_biases[:, :, None, :])
+            qk_raw_out, qk_gate_mass = _dense_rw_output_sharded(
+                primary_local,
+                primary_local_norm,
+                local_primary_valid,
+                local_qk_reads,
+                local_qk_writes,
+                primary_qk_tau,
+                max_chunk_size=max_chunk_size_qk,
+                soft_gate_temperature=qk_temperature,
+                soft_gate_boundary_power=boundary_power,
+                execution_prune_eps=execution_prune_eps,
+                srw_composition_mode=composition_mode,
+                heat_kernel_beta=heat_kernel_beta,
+                effective_active_eps=soft_gate_effective_active_eps,
+                throughput_bf16=throughput_bf16)
 
-        primary_v_tau = _apply_operation_space_tau_map(
-            primary_local, v_tau_kernel, v_tau_bias)
-        v_raw_out, v_gate_mass = _dense_rw_output_sharded(
-            primary_local,
-            primary_local_norm,
-            primary_valid,
-            v_read[None, ...],
-            v_write[None, ...],
-            primary_v_tau[None, ...],
-            max_chunk_size=max_chunk_size_v,
-            soft_gate_temperature=v_temperature,
-            soft_gate_boundary_power=boundary_power,
-            execution_prune_eps=execution_prune_eps,
-            srw_composition_mode=composition_mode,
-            heat_kernel_beta=heat_kernel_beta,
-            effective_active_eps=soft_gate_effective_active_eps,
-            throughput_bf16=throughput_bf16)
-        grouped_raw_out = jnp.concatenate(
-            (qk_raw_out, v_raw_out), axis=0)
-        grouped_gate_mass = jnp.concatenate(
-            (qk_gate_mass, v_gate_mass), axis=0)
-        grouped_raw_out = ad_checkpoint.checkpoint_name(
-            grouped_raw_out,
-            name=_BUNDLE_RW_RAW_OUT_CHECKPOINT_NAME)
-        grouped_gate_mass = ad_checkpoint.checkpoint_name(
-            grouped_gate_mass,
-            name=_BUNDLE_RW_GATE_MASS_CHECKPOINT_NAME)
-        _, grouped_gate_den = _global_dense_rw_den_sharded(
-            grouped_gate_mass, den_powers, composition_mode)
-        primary_results = (
-            grouped_raw_out / grouped_gate_den).astype(jnp.float32)
-        primary_block_output = writeback_dot(
-            "amtr,mrd->amtd",
-            primary_results
-            * primary_weight[None, ..., None]
-            * route_scales[:, None, ...],
-            space_write_proj).astype(jnp.float32)
-        primary_block_output = jnp.where(
-            primary_valid[None, ..., None],
-            primary_block_output,
-            0.0)
-        initial_output = jnp.zeros(
-            (3, token_count, d_model), dtype=jnp.float32
-        ).at[:, primary_token, :].add(primary_block_output)
+            primary_v_tau = _apply_operation_space_tau_map(
+                primary_local, local_v_tau_kernel, local_v_tau_bias)
+            v_raw_out, v_gate_mass = _dense_rw_output_sharded(
+                primary_local,
+                primary_local_norm,
+                local_primary_valid,
+                local_v_read[None, ...],
+                local_v_write[None, ...],
+                primary_v_tau[None, ...],
+                max_chunk_size=max_chunk_size_v,
+                soft_gate_temperature=v_temperature,
+                soft_gate_boundary_power=boundary_power,
+                execution_prune_eps=execution_prune_eps,
+                srw_composition_mode=composition_mode,
+                heat_kernel_beta=heat_kernel_beta,
+                effective_active_eps=soft_gate_effective_active_eps,
+                throughput_bf16=throughput_bf16)
+            grouped_raw_out = jnp.concatenate(
+                (qk_raw_out, v_raw_out), axis=0)
+            grouped_gate_mass = jnp.concatenate(
+                (qk_gate_mass, v_gate_mass), axis=0)
+            grouped_raw_out = ad_checkpoint.checkpoint_name(
+                grouped_raw_out,
+                name=_BUNDLE_RW_RAW_OUT_CHECKPOINT_NAME)
+            grouped_gate_mass = ad_checkpoint.checkpoint_name(
+                grouped_gate_mass,
+                name=_BUNDLE_RW_GATE_MASS_CHECKPOINT_NAME)
+            _, grouped_gate_den = _global_dense_rw_den_sharded(
+                grouped_gate_mass, den_powers, composition_mode)
+            primary_results = (
+                grouped_raw_out / grouped_gate_den).astype(jnp.float32)
+            primary_block_output = writeback_dot(
+                "amtr,mrd->amtd",
+                primary_results
+                * local_primary_weight[None, ..., None]
+                * route_scales[:, None, ...],
+                local_space_write_proj).astype(jnp.float32)
+            return jnp.where(
+                local_primary_valid[None, ..., None],
+                primary_block_output,
+                0.0)
+
+        primary_token = packed_metadata["primary_token_id"]
+        primary_valid = packed_metadata["primary_token_valid"]
+        if primary_group_size is None:
+            primary_block_output = execute_primary_spaces(
+                primary_token,
+                primary_valid,
+                primary_weight,
+                space_read_proj,
+                space_write_proj,
+                qk_tau_kernels,
+                qk_tau_biases,
+                v_tau_kernel,
+                v_tau_bias,
+                qk_reads,
+                qk_writes,
+                v_read,
+                v_write)
+            initial_output = jnp.zeros(
+                (3, token_count, d_model), dtype=jnp.float32
+            ).at[:, primary_token, :].add(primary_block_output)
+        else:
+            primary_group_size = int(primary_group_size)
+            if (
+                    primary_group_size <= 0
+                    or n_spaces % primary_group_size):
+                raise ValueError(
+                    "exact attention primary group size must positively "
+                    "divide the operation-space count")
+            primary_group_count = n_spaces // primary_group_size
+
+            def primary_group_step(local_output, group_index):
+                space_start = group_index * primary_group_size
+                group_primary_token = jax.lax.dynamic_slice_in_dim(
+                    primary_token,
+                    space_start,
+                    primary_group_size,
+                    axis=0)
+                group_primary_valid = jax.lax.dynamic_slice_in_dim(
+                    primary_valid,
+                    space_start,
+                    primary_group_size,
+                    axis=0)
+                group_primary_weight = jax.lax.dynamic_slice_in_dim(
+                    primary_weight,
+                    space_start,
+                    primary_group_size,
+                    axis=0)
+                group_space_read_proj = jax.lax.dynamic_slice_in_dim(
+                    space_read_proj,
+                    space_start,
+                    primary_group_size,
+                    axis=0)
+                group_space_write_proj = jax.lax.dynamic_slice_in_dim(
+                    space_write_proj,
+                    space_start,
+                    primary_group_size,
+                    axis=0)
+                group_qk_tau_kernels = jax.lax.dynamic_slice_in_dim(
+                    qk_tau_kernels,
+                    space_start,
+                    primary_group_size,
+                    axis=1)
+                group_qk_tau_biases = jax.lax.dynamic_slice_in_dim(
+                    qk_tau_biases,
+                    space_start,
+                    primary_group_size,
+                    axis=1)
+                group_v_tau_kernel = jax.lax.dynamic_slice_in_dim(
+                    v_tau_kernel,
+                    space_start,
+                    primary_group_size,
+                    axis=0)
+                group_v_tau_bias = jax.lax.dynamic_slice_in_dim(
+                    v_tau_bias,
+                    space_start,
+                    primary_group_size,
+                    axis=0)
+                group_qk_reads = jax.lax.dynamic_slice_in_dim(
+                    qk_reads,
+                    space_start,
+                    primary_group_size,
+                    axis=1)
+                group_qk_writes = jax.lax.dynamic_slice_in_dim(
+                    qk_writes,
+                    space_start,
+                    primary_group_size,
+                    axis=1)
+                group_v_read = jax.lax.dynamic_slice_in_dim(
+                    v_read,
+                    space_start,
+                    primary_group_size,
+                    axis=0)
+                group_v_write = jax.lax.dynamic_slice_in_dim(
+                    v_write,
+                    space_start,
+                    primary_group_size,
+                    axis=0)
+                group_output = execute_primary_spaces(
+                    group_primary_token,
+                    group_primary_valid,
+                    group_primary_weight,
+                    group_space_read_proj,
+                    group_space_write_proj,
+                    group_qk_tau_kernels,
+                    group_qk_tau_biases,
+                    group_v_tau_kernel,
+                    group_v_tau_bias,
+                    group_qk_reads,
+                    group_qk_writes,
+                    group_v_read,
+                    group_v_write)
+                return local_output.at[
+                    :, group_primary_token, :].add(group_output), None
+
+            initial_output, _ = jax.lax.scan(
+                primary_group_step,
+                jnp.zeros(
+                    (3, token_count, d_model), dtype=jnp.float32),
+                jnp.arange(primary_group_count))
 
         def task_group_step(local_output, group_index):
             task_start = group_index * task_group_size
@@ -4372,7 +4507,9 @@ def _make_sharded_attention_space_bundle_dense(
             controls,
             packed_metadata,
             packed["primary_routing_weight"],
-            packed["overflow_task_routing_weight"])
+            packed["overflow_task_routing_weight"],
+            primary_group_size=(
+                _EXACT_SPACE_FORWARD_PRIMARY_GROUP_SIZE))
 
     @jax.custom_vjp
     def execute_with_exact_space_backward(
@@ -4745,6 +4882,8 @@ def _make_sharded_attention_space_bundle_dense(
             "exact_top2_space_batched_dense_with_overflow")
         kernel._v4174_forward_bucket_capacity = (
             _EXACT_SPACE_BACKWARD_BUCKET_CAPACITY)
+        kernel._v4174_forward_primary_group_size = (
+            _EXACT_SPACE_FORWARD_PRIMARY_GROUP_SIZE)
         kernel._v4174_forward_overflow_task_group_size = (
             _EXACT_SPACE_BACKWARD_TASK_GROUP_SIZE)
         kernel._v4174_packed_entry_capacity = (
@@ -5179,7 +5318,9 @@ def _make_sharded_rst_space_bundle_dense(
             controls,
             packed_metadata,
             primary_weight,
-            overflow_weight):
+            overflow_weight,
+            *,
+            primary_group_size: Optional[int] = None):
         (space_read_proj, space_write_proj,
          tau_kernel, tau_bias) = state_params
         read_vectors, write_vectors = operator_params
@@ -5192,50 +5333,150 @@ def _make_sharded_rst_space_bundle_dense(
         task_capacity = int(
             packed_metadata["overflow_task_space_id"].shape[0])
         task_groups = task_capacity // task_group_size
+
+        def execute_primary_spaces(
+                local_primary_token,
+                local_primary_valid,
+                local_primary_weight,
+                local_space_read_proj,
+                local_space_write_proj,
+                local_tau_kernel,
+                local_tau_bias,
+                local_read_vectors,
+                local_write_vectors):
+            primary_state = global_state[local_primary_token]
+            primary_local = projection_dot(
+                "mtd,mdr->mtr", primary_state, local_space_read_proj
+            ).astype(jnp.float32)
+            primary_local_norm = jnp.maximum(
+                jnp.linalg.norm(primary_local, axis=-1, keepdims=True),
+                jnp.float32(RW_FORWARD_NORM_EPS))
+            primary_tau = _apply_operation_space_tau_map(
+                primary_local, local_tau_kernel, local_tau_bias)
+            raw_out, gate_mass = _dense_rw_output_sharded(
+                primary_local,
+                primary_local_norm,
+                local_primary_valid,
+                local_read_vectors[None, ...],
+                local_write_vectors[None, ...],
+                primary_tau[None, ...],
+                max_chunk_size=max_chunk_size,
+                soft_gate_temperature=temperature,
+                soft_gate_boundary_power=boundary_power,
+                execution_prune_eps=execution_prune_eps,
+                srw_composition_mode=composition_mode,
+                heat_kernel_beta=heat_kernel_beta,
+                effective_active_eps=soft_gate_effective_active_eps,
+                throughput_bf16=throughput_bf16)
+            raw_out = ad_checkpoint.checkpoint_name(
+                raw_out, name=_BUNDLE_RW_RAW_OUT_CHECKPOINT_NAME)
+            gate_mass = ad_checkpoint.checkpoint_name(
+                gate_mass, name=_BUNDLE_RW_GATE_MASS_CHECKPOINT_NAME)
+            _, gate_den = _global_dense_rw_den_sharded(
+                gate_mass, den_power, composition_mode)
+            primary_results = (raw_out / gate_den).astype(jnp.float32)
+            primary_block_output = writeback_dot(
+                "mtr,mrd->mtd",
+                primary_results[0]
+                * local_primary_weight[..., None]
+                * jnp.asarray(route_scale, dtype=jnp.float32),
+                local_space_write_proj).astype(jnp.float32)
+            return jnp.where(
+                local_primary_valid[..., None],
+                primary_block_output,
+                0.0)
+
         primary_token = packed_metadata["primary_token_id"]
         primary_valid = packed_metadata["primary_token_valid"]
-        primary_state = global_state[primary_token]
-        primary_local = projection_dot(
-            "mtd,mdr->mtr", primary_state, space_read_proj
-        ).astype(jnp.float32)
-        primary_local_norm = jnp.maximum(
-            jnp.linalg.norm(primary_local, axis=-1, keepdims=True),
-            jnp.float32(RW_FORWARD_NORM_EPS))
-        primary_tau = _apply_operation_space_tau_map(
-            primary_local, tau_kernel, tau_bias)
-        raw_out, gate_mass = _dense_rw_output_sharded(
-            primary_local,
-            primary_local_norm,
-            primary_valid,
-            read_vectors[None, ...],
-            write_vectors[None, ...],
-            primary_tau[None, ...],
-            max_chunk_size=max_chunk_size,
-            soft_gate_temperature=temperature,
-            soft_gate_boundary_power=boundary_power,
-            execution_prune_eps=execution_prune_eps,
-            srw_composition_mode=composition_mode,
-            heat_kernel_beta=heat_kernel_beta,
-            effective_active_eps=soft_gate_effective_active_eps,
-            throughput_bf16=throughput_bf16)
-        raw_out = ad_checkpoint.checkpoint_name(
-            raw_out, name=_BUNDLE_RW_RAW_OUT_CHECKPOINT_NAME)
-        gate_mass = ad_checkpoint.checkpoint_name(
-            gate_mass, name=_BUNDLE_RW_GATE_MASS_CHECKPOINT_NAME)
-        _, gate_den = _global_dense_rw_den_sharded(
-            gate_mass, den_power, composition_mode)
-        primary_results = (raw_out / gate_den).astype(jnp.float32)
-        primary_block_output = writeback_dot(
-            "mtr,mrd->mtd",
-            primary_results[0]
-            * primary_weight[..., None]
-            * jnp.asarray(route_scale, dtype=jnp.float32),
-            space_write_proj).astype(jnp.float32)
-        primary_block_output = jnp.where(
-            primary_valid[..., None], primary_block_output, 0.0)
-        initial_output = jnp.zeros(
-            (token_count, d_model), dtype=jnp.float32
-        ).at[primary_token, :].add(primary_block_output)
+        if primary_group_size is None:
+            primary_block_output = execute_primary_spaces(
+                primary_token,
+                primary_valid,
+                primary_weight,
+                space_read_proj,
+                space_write_proj,
+                tau_kernel,
+                tau_bias,
+                read_vectors,
+                write_vectors)
+            initial_output = jnp.zeros(
+                (token_count, d_model), dtype=jnp.float32
+            ).at[primary_token, :].add(primary_block_output)
+        else:
+            primary_group_size = int(primary_group_size)
+            if (
+                    primary_group_size <= 0
+                    or n_spaces % primary_group_size):
+                raise ValueError(
+                    "exact RST primary group size must positively divide "
+                    "the operation-space count")
+            primary_group_count = n_spaces // primary_group_size
+
+            def primary_group_step(local_output, group_index):
+                space_start = group_index * primary_group_size
+                group_primary_token = jax.lax.dynamic_slice_in_dim(
+                    primary_token,
+                    space_start,
+                    primary_group_size,
+                    axis=0)
+                group_primary_valid = jax.lax.dynamic_slice_in_dim(
+                    primary_valid,
+                    space_start,
+                    primary_group_size,
+                    axis=0)
+                group_primary_weight = jax.lax.dynamic_slice_in_dim(
+                    primary_weight,
+                    space_start,
+                    primary_group_size,
+                    axis=0)
+                group_space_read_proj = jax.lax.dynamic_slice_in_dim(
+                    space_read_proj,
+                    space_start,
+                    primary_group_size,
+                    axis=0)
+                group_space_write_proj = jax.lax.dynamic_slice_in_dim(
+                    space_write_proj,
+                    space_start,
+                    primary_group_size,
+                    axis=0)
+                group_tau_kernel = jax.lax.dynamic_slice_in_dim(
+                    tau_kernel,
+                    space_start,
+                    primary_group_size,
+                    axis=0)
+                group_tau_bias = jax.lax.dynamic_slice_in_dim(
+                    tau_bias,
+                    space_start,
+                    primary_group_size,
+                    axis=0)
+                group_read_vectors = jax.lax.dynamic_slice_in_dim(
+                    read_vectors,
+                    space_start,
+                    primary_group_size,
+                    axis=0)
+                group_write_vectors = jax.lax.dynamic_slice_in_dim(
+                    write_vectors,
+                    space_start,
+                    primary_group_size,
+                    axis=0)
+                group_output = execute_primary_spaces(
+                    group_primary_token,
+                    group_primary_valid,
+                    group_primary_weight,
+                    group_space_read_proj,
+                    group_space_write_proj,
+                    group_tau_kernel,
+                    group_tau_bias,
+                    group_read_vectors,
+                    group_write_vectors)
+                return local_output.at[
+                    group_primary_token, :].add(group_output), None
+
+            initial_output, _ = jax.lax.scan(
+                primary_group_step,
+                jnp.zeros(
+                    (token_count, d_model), dtype=jnp.float32),
+                jnp.arange(primary_group_count))
 
         def task_group_step(local_output, group_index):
             task_start = group_index * task_group_size
@@ -5360,7 +5601,9 @@ def _make_sharded_rst_space_bundle_dense(
             controls,
             packed_metadata,
             packed["primary_routing_weight"],
-            packed["overflow_task_routing_weight"])
+            packed["overflow_task_routing_weight"],
+            primary_group_size=(
+                _EXACT_SPACE_FORWARD_PRIMARY_GROUP_SIZE))
 
     @jax.custom_vjp
     def execute_with_exact_space_backward(
@@ -5673,6 +5916,8 @@ def _make_sharded_rst_space_bundle_dense(
             "exact_top2_space_batched_dense_with_overflow")
         kernel._v4174_forward_bucket_capacity = (
             _EXACT_SPACE_BACKWARD_BUCKET_CAPACITY)
+        kernel._v4174_forward_primary_group_size = (
+            _EXACT_SPACE_FORWARD_PRIMARY_GROUP_SIZE)
         kernel._v4174_forward_overflow_task_group_size = (
             _EXACT_SPACE_BACKWARD_TASK_GROUP_SIZE)
         kernel._v4174_packed_entry_capacity = (
@@ -5809,6 +6054,12 @@ def _validate_v4174_sharded_fns(
                 raise ValueError(
                     f"v4174 {name} exact_top2 forward bucket capacity "
                     f"must be {_EXACT_SPACE_BACKWARD_BUCKET_CAPACITY}")
+            if int(getattr(
+                    kernel, "_v4174_forward_primary_group_size", 0
+            )) != _EXACT_SPACE_FORWARD_PRIMARY_GROUP_SIZE:
+                raise ValueError(
+                    f"v4174 {name} exact_top2 forward primary group size "
+                    f"must be {_EXACT_SPACE_FORWARD_PRIMARY_GROUP_SIZE}")
             if int(getattr(
                     kernel,
                     "_v4174_forward_overflow_task_group_size",
-- 
2.54.0.windows.1

